{
  "scripts": {
    "validate": "ajv compile --spec=draft2019 --verbose --all-errors -r storage.schema.json -r iscsi.schema.json -s profile.schema.json"
  },
  "dependencies": {
    "ajv-cli": "^5.0.0"
  }
}
